server:
  port: 8080
  servlet:
    context-path: /identity


spring:
  datasource:
    url: "jdbc:sqlserver://localhost:1434;databaseName=MyDB;encrypt=true;trustServerCertificate=true"
    username: "sa"
    password: "Aesx5099@@"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.SQLServerDialect